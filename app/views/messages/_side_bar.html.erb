<div class="side_bar">
  <div class="header side_head">
    <%= link_to current_user.name ,edit_user_path(current_user) ,class: "user_name"%>
    <%= link_to "チャットを作成する" ,new_room_path(current_user) ,class: "chat_create_btn btn"%>
  </div>
  <div class="side_line">
    <div class="room_name_index">
      <% current_user.rooms.each do |room| %>
        <%= link_to room.name ,room_messages_path(room) ,class: "room_name btn"%>
        <%# <a class="room_name btn" href="#"><%= room.name %></a>
      <% end %>
    </div>
  </div>
</div>